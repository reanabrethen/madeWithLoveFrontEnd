//list component is going to map through 
//feed it in props each individual meal

//make sure list has layout 
//when

import React from 'react'

function RecipeList({}) {
  return (
    <div>RecipeList</div>
  )
}

export default RecipeList